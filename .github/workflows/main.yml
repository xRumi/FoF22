name: deploy
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Executing remote command
      uses: appleboy/ssh-action@master
      with:
        host: '88.99.83.158'
        username: 'root'
        password: '_ubuntu_1533_rumi'
        script: cd /root/FoF22 && git pull